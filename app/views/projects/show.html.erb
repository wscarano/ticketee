<h2><%= @project.name %></h2>
<% title(@project.name, "Projects") %>

<%= link_to "Edit Project", edit_project_path(@project) %>

<%= link_to "Delete Project", @project, :method => :delete %>,
	:confirm => "Are you sure want to delete this project?" %>
	
	